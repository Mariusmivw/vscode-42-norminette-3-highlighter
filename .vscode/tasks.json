// A task runner that calls a custom npm script that compiles the extension.
{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "npm",
			"script": "test-compile",
			"group": "build",
			"problemMatcher": [],
			"label": "npm: test-compile",
			"detail": "tsc -p ./"
		}
	]
}
